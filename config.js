const dbOptions = {
    host:      'host-url',
    user:       'users-name',
    password: 'users-password',
    port:       'some-port',
    database:       'employeeslist' //don't change
};

module.exports = {
    dbOptions
};
